<Project>
  <Import Project="$(MSBuildThisFileDirectory)..\Directory.Build.targets" />
  <Import Project="$(MSBuildThisFileDirectory)Archive.targets" Condition="'$(IsArchivable)' == 'true'" />
  <Import Project="$(MSBuildThisFileDirectory)Pack.targets" Condition="'$(IsPackable)' == 'true'" />
</Project>
